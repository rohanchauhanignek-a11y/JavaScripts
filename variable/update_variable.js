//Use assignment operators to update a variable.
function updata(a)
    {
        a+=10;
        console.log(`After updateing value: ${a}`)
    }
let a=1;
console.log(`Before updateing value: ${a}`)
updata(a);

